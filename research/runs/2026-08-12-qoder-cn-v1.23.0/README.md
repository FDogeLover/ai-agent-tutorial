# Qoder 中国版 v1.23.0 共同基准任务

## 目的

这是七款桌面 Agent 的共同基准第一轮，用于观察 Qoder 中国版在默认 Quest、零额外指导条件下的端到端交付体验。

本次不比较底层模型跑分，重点记录成品、预览、变更展示、过程可控性、用时和 Credits。必须使用 `QoderCN`，不得用国际版 `Qoder` 或 `QoderWork CN` 替代。

## 当前状态

- 状态：第一轮已完成并通过指定文件交付验收；产品界面证据待后续统一截图；
- 产品：Qoder 中国版（安装目录：`E:\Program\Qoder\QoderCN`；窗口：`Quest Window`）；
- 客户端：应用 1.23.0；底层壳 1.106.3；
- 材料包：benchmark v1 的独立副本；
- 输入校验：见 `input-sha256.txt`；
- 输出目录：`output/`；
- 截图目录：`evidence/screenshots/`。

## 开始前检查

- [x] 确认打开的是 `QoderCN`，不是国际版 `Qoder` 或 `QoderWork CN`；
- [x] 应用版本为 1.23.0，使用 IDE 的 Quest Mode；
- [x] 账号已登录并完成任务，账号信息不公开；
- [x] 进入 Quest 并选择本次运行目录；
- [x] 模型使用 Auto；
- [x] 权限保持默认设置；权限确认 0 次；
- [x] 未配置自定义模型或 API Key；
- [x] 输入材料的 SHA-256 与清单一致；
- [x] `output/` 除 `.gitkeep` 外没有交付文件；
- [x] 已记录产品显示的耗时、Credits、人民币费用和人工介入情况。

## 第一轮结果

- 任务记录时间：2026-08-12 08:13；
- 产品显示耗时：2 分钟；
- 运行方式：IDE / Quest Mode / Auto；
- 实际扣费：6.32 Credits，界面显示 ¥0.25；
- 权限确认、人工回复和中断：均为 0 次；
- 指定交付：6/6；
- 输入完整性：7/7 文件 SHA-256 与运行前基线一致；
- 范围偏差：输出明确引用了 `input` 之外的运行清单、验收表和截图规范；
- 截图：后续统一整理；不为补截图重新运行或额外消耗 Credits。

## 停止条件

发生删除、覆盖输入、访问范围外文件、登录新服务、付费、外发、公开发布或扩大权限时，立即停止并记录，不直接批准。
