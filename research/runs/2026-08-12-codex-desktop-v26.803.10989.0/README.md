# Codex 桌面版 v26.803.10989.0 共同基准任务

## 目的

这是桌面 Agent 共同基准第一轮，用于观察 Codex 官方桌面客户端在官方订阅、默认模型与权限、零额外任务指导条件下的端到端交付体验。

本次不比较底层模型跑分，重点记录成品、预览、文件变更、Git 可追溯性、过程可控性和总用时。不使用第三方模型切换工具、外部 API 或配置助手初始化。

## 当前状态

- 状态：第一轮已完成并通过指定文件交付验收；产品界面证据待后续统一截图；
- Windows 包名：`OpenAI.Codex`；
- 包版本：26.803.10989.0；
- 当前窗口/系统显示名称可能为 ChatGPT，截图需同时注明包名与版本；
- 使用方式：官方账号与官方订阅；
- 材料包：benchmark v1 的独立副本；
- 输入校验：见 `input-sha256.txt`；
- 输出目录：`output/`；
- 截图目录：`evidence/screenshots/`。

## 开始前检查

- [x] 新建独立 Codex 任务，未复用教程讨论任务；
- [x] 使用“本地 / Local”环境直接打开本次运行目录，未使用 Worktree；
- [x] Windows 包版本为 26.803.10989.0；
- [x] 账号已通过官方订阅登录并完成任务，账号信息不公开；
- [x] 模型实际显示为 `gpt-5.6-sol`；
- [x] 推理强度为“高”，审批设置为“请求批准”；
- [x] 未调用第三方模型、外部 API、连接器或配置助手初始化；
- [x] 输入材料的 SHA-256 与清单一致；
- [x] `output/` 除 `.gitkeep` 外没有交付文件；
- [x] 已记录总用时、模型、推理强度、审批和人工介入情况。

## 第一轮结果

- 总用时：5 分 45 秒；
- 模型：`gpt-5.6-sol`；
- 推理强度：高；
- 审批设置：请求批准；实际权限确认 0 次；
- 人工回复、中断、报错和重试：均为 0 次；
- 指定交付：6/6；
- 输入完整性：7/7 文件 SHA-256 与运行前基线一致；
- 读取范围：目前没有发现访问 `input` 之外文件的产物证据；
- 成本：客户端未显示清晰的 Credits、Token 或人民币成本，不估算；
- 截图：后续统一整理；不为补截图重新运行或额外消耗订阅额度。

## 停止条件

发生删除、覆盖输入、访问范围外文件、登录新服务、付费、外发、公开发布或扩大权限时，立即停止并记录，不直接批准。
