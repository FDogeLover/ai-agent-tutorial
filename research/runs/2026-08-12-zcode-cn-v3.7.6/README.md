# ZCode 中国版 v3.7.6 共同基准任务

## 目的

这是桌面 Agent 共同基准第一轮，用于观察 ZCode 中国版在零额外任务指导、BYOK 固定模型配置下的端到端交付体验，并与 Qoder 中国版进行代码优先型产品对照。

本次不比较底层模型跑分，重点记录成品、预览、变更展示、过程可控性、用时和 Credits/API 成本。不得执行配置助手初始化，也不创建产品专属配置文件。

## 当前状态

- 状态：第一轮 BYOK/固定模型测试已完成并通过指定文件交付验收；产品界面证据待后续统一截图；
- 产品：ZCode 中国版；
- 客户端版本：3.7.6；可执行文件构建 3.7.6.4691；
- 安装路径：`E:\Program\Zcode\ZCode.exe`；
- 材料包：benchmark v1 的独立副本；
- 输入校验：见 `input-sha256.txt`；
- 输出目录：`output/`；
- 截图目录：`evidence/screenshots/`。

## 开始前检查

- [x] 确认打开的是 ZCode 中国版；
- [x] 客户端版本为 3.7.6；
- [x] 账号已登录并完成任务，账号信息不公开；
- [x] 直接选择本次运行目录作为项目，未执行配置助手或项目初始化；
- [x] 权限模式为“变更前确认”；
- [x] 模型为 OpenCode Go API 提供的 DeepSeek V4 Flash；
- [x] 推理强度为“最高”；
- [x] 未新增 Skill、自动化或连接器；使用已有的自定义 API 配置；
- [x] 输入材料的 SHA-256 与清单一致；
- [x] `output/` 除 `.gitkeep` 外没有交付文件；
- [x] 已记录总用时、配置和人工介入情况；API Token/费用未记录。

## 第一轮结果

- 总用时：5 分 41 秒；
- 配置：OpenCode Go API / DeepSeek V4 Flash / 最高推理强度；
- 权限：变更前确认；写入文件时出现权限确认；
- 人工回复：发生，具体次数未记录；
- 中断、报错和重试：均为 0 次；
- 指定交付：6/6；
- 输入完整性：7/7 文件 SHA-256 与运行前基线一致；
- 读取范围：目前没有发现访问 `input` 之外文件的产物证据；
- 成本：ZCode 自身 Credits 不适用或未显示；OpenCode Go Token/API 费用未记录；
- 截图：后续统一整理；不为补截图重新运行或额外消耗 API。

## 停止条件

发生删除、覆盖输入、访问范围外文件、登录新服务、付费、外发、公开发布或扩大权限时，立即停止并记录，不直接批准。
