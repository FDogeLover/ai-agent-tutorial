# QoderWork CN v0.9.12 共同基准任务

## 目的

这是桌面 Agent 共同基准的扩展样本，用于观察 QoderWork CN 在默认模式、零额外指导条件下的端到端交付体验，并与代码优先型的 Qoder CN 分开比较。

本次不比较底层模型跑分，重点记录成品、预览、变更展示、过程可控性、用时和 Credits。必须使用 `QoderWork CN`，不得用 `QoderCN` 或国际版 `Qoder` 替代。

## 当前状态

- 状态：扩展样本第一轮已完成并通过指定文件交付验收；产品界面证据待后续统一截图；
- 产品：QoderWork CN；
- 客户端版本：0.9.12；
- 安装路径：`E:\Program\Qoder\QoderWork CN\QoderWork CN.exe`；
- 材料包：benchmark v1 的独立副本；
- 输入校验：见 `input-sha256.txt`；
- 输出目录：`output/`；
- 截图目录：`evidence/screenshots/`。

## 开始前检查

- [x] 确认打开的是 `QoderWork CN`，不是 `QoderCN` 或国际版 `Qoder`；
- [x] 客户端版本为 0.9.12；
- [x] 账号已登录并完成任务，账号信息不公开；
- [x] 选择本次运行目录作为工作空间或项目；
- [x] 使用“智能体 / Auto”；
- [x] 权限保持默认设置；权限确认 0 次；
- [x] 未配置自定义模型或 API Key；
- [x] 输入材料的 SHA-256 与清单一致；
- [x] `output/` 除 `.gitkeep` 外没有交付文件；
- [x] 已记录两条界面计费明细和人工介入情况。

## 第一轮结果

- 第一条记录：2026-08-12 08:25，2 分钟，7.92 Credits，¥0.31；
- 第二条记录：2026-08-12 08:27，不足 1 分钟，0.76 Credits，¥0.03；
- 合计：8.68 Credits，¥0.34；界面显示时长合计不足 3 分钟；
- 运行方式：QoderWork / 智能体 / Auto；
- 权限确认、人工回复和中断：均为 0 次；
- 指定交付：6/6；
- 输入完整性：7/7 文件 SHA-256 与运行前基线一致；
- 范围偏差：输出明确引用了 `input` 之外的 `run-record.md` 和验收表；
- 截图：后续统一整理；不为补截图重新运行或额外消耗 Credits。

## 停止条件

发生删除、覆盖输入、访问范围外文件、登录新服务、付费、外发、公开发布或扩大权限时，立即停止并记录，不直接批准。
